spring.application.name=trusthire-ai

server.port=8080

# Ollama LLM configuration
ollama.base-url=http://localhost:11434
ollama.model=llama3.2
ollama.connect-timeout-seconds=3
ollama.read-timeout-seconds=60

# HMAC signing secret — override with TRUSTHIRE_SIGNING_SECRET env var in production
# Example: export TRUSTHIRE_SIGNING_SECRET=your-actual-secret
app.signing.secret=${TRUSTHIRE_SIGNING_SECRET:trusthire-dev-secret-change-in-prod}

# CORS — frontend dev server
app.cors.allowed-origin=http://localhost:3000

# Logging — never log CGPA, backlogs, secrets, or full prompts
logging.level.com.trusthire=INFO
logging.level.root=WARN
