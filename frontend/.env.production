# Production environment — baked into the Next.js bundle at build time.
# Next.js reads this automatically during `npm run build`.
# The /api prefix is rewritten by next.config.ts to http://localhost:8081/api/*
NEXT_PUBLIC_API_BASE_URL=/api
