# Production environment — baked into the Next.js bundle at build time.
# All API paths in lib/api.ts already include the /api prefix (e.g. /api/drives),
# so BASE must be empty — NOT "/api" (which would double it to /api/api/drives).
NEXT_PUBLIC_API_BASE_URL=
